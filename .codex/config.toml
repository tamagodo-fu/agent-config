# sanitized copy of ~/.codex/config.toml for publication.
# Removed machine-local state: [projects.*] trust list, [hooks.state.*],
# [marketplaces.*], [notice.*], [tui.*], and auto-injected MCP servers
# (node_repl / computer-use, managed by the ChatGPT desktop app).

model = "gpt-6-astra"
model_reasoning_effort = "medium"
personality = "pragmatic"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"
project_doc_fallback_filenames = ["CLAUDE.md"]
service_tier = "default"

# agmsg (cross-agent messaging) needs write access to its SQLite/team state
[sandbox_workspace_write]
writable_roots = ["~/.agents/skills/agmsg/db", "~/.agents/skills/agmsg/teams", "~/.agents/skills/agmsg/run"]
network_access = true

[features]
multi_agent = true
multi_agent_v2 = true
apps = true
prevent_idle_sleep = true
hooks = true
js_repl = false
memories = true

[agents]
enabled = true
max_concurrent_threads_per_session = 3

[agents.explorer]
config_file = "role-configs/explorer.toml"
description = "Bounded read-only local exploration using GPT-6 Luna max."

[agents.worker]
config_file = "role-configs/worker.toml"
description = "Bounded implementation and affected tests using GPT-6 Sol high."

[agents.researcher_worker]
config_file = "role-configs/researcher.toml"
description = "Focused primary-source research using GPT-6 Luna max."

[agents.reviewer]
config_file = "role-configs/reviewer.toml"
description = "Independent high-risk review using GPT-6 Astra xhigh."

[plugins."google-calendar@openai-curated"]
enabled = true

[plugins."gmail@openai-curated"]
enabled = true

[plugins."github@openai-curated"]
enabled = true

[plugins."google-drive@openai-curated"]
enabled = true

[plugins."documents@openai-primary-runtime"]
enabled = true

[plugins."spreadsheets@openai-primary-runtime"]
enabled = true

[plugins."presentations@openai-primary-runtime"]
enabled = true

[plugins."computer-use@openai-bundled"]
enabled = true

[plugins."remotion@openai-curated"]
enabled = true

[plugins."hyperframes@openai-curated"]
enabled = true

[plugins."build-ios-apps@openai-curated"]
enabled = true

[plugins."chrome@openai-bundled"]
enabled = true

[plugins."pdf@openai-primary-runtime"]
enabled = true

[plugins."template-creator@openai-primary-runtime"]
enabled = true

[plugins."visualize@openai-bundled"]
enabled = true

[plugins."sites@openai-bundled"]
enabled = true

[plugins."browser@openai-bundled"]
enabled = true

# local gbrain knowledge-base MCP (same definition as Claude Code / Devin)
[mcp_servers.gbrain]
command = "~/bin/gbrain"
args = ["serve"]

[desktop]
preventSleepWhileRunning = true
keepRemoteControlAwakeWhilePluggedIn = true
followUpQueueMode = "queue"
dock-icon-preference = "codex-system"
hotkey-window-projectless-default-enabled = true
show-context-window-usage = true
composerEnterBehavior = "cmdAlways"

[memories]
generate_memories = true
use_memories = true
disable_on_external_context = true
